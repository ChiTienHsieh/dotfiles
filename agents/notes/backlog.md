# Backlog（想做但還沒排程的事）

## 收件規則

`issue this:` 只收件、不實作。先判斷 canonical owner，不看目前 cwd：

- 明確屬於某個 GitHub repo 且適合公開 → create/update canonical issue。
- 其他情況 → append 到 `~/Documents/Codex/BACKLOG.md`；不確定時預設放本機。
- 記錄簡短標題、Why、Next、來源 task URL 與日期，完成後立即停止。

## 已收項目

一行一項，做掉就刪。跟 daily-loop / wrap 的提案互補：這裡放 user 點頭過「之後再說」的項目。

- 2026-07-10: 重新設定 oracle CLI 並評估是否需要 oracle skill。原 shared/oracle skill 已移除（使用量近零＋觸發全靠工具名）；若之後 hard-bug 第二意見需求回來，先設定好 oracle CLI 本體，再決定要不要包 skill。
