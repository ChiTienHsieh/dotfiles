# Git-tracked Codex settings that install.sh overlays onto the runtime-owned
# ~/.codex/config.toml. Keep this file small and limited to stable preferences.

[agents]
enabled = true
max_concurrent_threads_per_session = 100
default_subagent_model = "gpt-5.6-luna"
default_subagent_reasoning_effort = "max"
