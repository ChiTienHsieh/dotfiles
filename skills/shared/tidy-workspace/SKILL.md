---
name: tidy-workspace
description: "Alias for the wrap workflow. Use when the user invokes $tidy-workspace or asks to tidy, clean up, or synchronize the current Git workspace, including dirty worktrees, local commits, stashes, remote changes, and safe session wrap-up."
---

# tidy-workspace — `$wrap` alias

Read `../wrap/SKILL.md` completely, then follow it as the workflow SSOT. Treat
this invocation exactly like an explicit `$wrap` request.

Do not copy, shorten, or reinterpret the wrap workflow here. If this alias and
`$wrap` ever conflict, follow `$wrap`; the user's current instructions still
take precedence over both skills.
