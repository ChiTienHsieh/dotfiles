interface:
  display_name: "tmux orchestration"
  short_description: "Human-opt-in interactive agent sessions in tmux."
  default_prompt: "Use $tmux-orchestration only if my current request explicitly asks you to use tmux; otherwise do not activate it."

policy:
  allow_implicit_invocation: false
