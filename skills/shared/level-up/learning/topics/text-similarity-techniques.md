# 文字相似度技術（pg_trgm、BM25、embedding）

狀態：`familiar`（概念推理正確；尚未在真實專案親手實作）

## 證據

- 能算 pg_trgm 的集合交集／聯集分數，並推出長字串分數反而下降的分母稀釋效應。
- 理解中文跨詞 trigram 不是 bug；能判斷同義但字面不同的詞效果普通，字面重疊的片段會拉回分數。
- 自行推出倒排索引把複雜度從文件數量級壓到詞彙數量級；修正過「pg_trgm 是暴力掃」的誤解，知道它也用 GIN index。
- 理解 BM25 的 TF 遞減與 IDF 稀有詞加權，能指出稀有詞貢獻最大。
- 能依維護成本選型：快層用 pg_trgm 零額外依賴直上，BM25 維護成本高可跳過；主動提出加 reranker 的投報率高於升級成 BM25，並自我修正為「要看資料」。
- 能區分「不出站」與「不裝額外 extension」兩種依賴限制。

## 下一步

pgvector 實作：embedding 怎麼存、怎麼查、怎麼選 model；或把 pg_trgm 快層真的寫進 SQL 與索引。
