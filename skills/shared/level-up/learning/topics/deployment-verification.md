# 部署、驗證與復原

狀態：`familiar`

## 證據

- 能區分不可變部署版本與可移動的網址指向，並主動查實際綁定資料糾正目錄名稱推論；尚無獨立 preview 部署證據。
- 能區分本機測試、正式部署與上線後檢查，不把前一階段通過當成後一階段已完成。
- 能指出同機備份仍共用故障範圍，並提出獨立異地副本；能辨認金鑰復原與存活警報是不同風險。
- 能說明本機 hook 可以被跳過、伺服器端 CI 才是跳不掉的防線；自行把「GitHub 命令部署平台」修正成「平台訂閱合併事件後自行部署」。

## 下一步

用假資料實作一次部署、回復與異地還原，分別讀回結果。
